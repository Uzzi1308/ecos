{
  "name": "ecos-entre-nosotros",
  "version": "1.0.0",
  "description": "Metroidvania emocional sobre relaciones",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html --public-url ./",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.60.0"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "parcel": "^2.9.3",
    "process": "^0.11.10"
  }
}
